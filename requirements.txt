# Firecracker OpenAI Code Generator Dependencies
# Firecracker project dependencies

# Configuration / runtime
hydra-core>=1.3.0
omegaconf>=2.3.0

# Web / API
fastapi>=0.95.0
uvicorn>=0.22.0
pydantic>=2.0.0,<3.0.0

# HTTP clients
requests>=2.28.0
httpx>=0.24.0

# Logging / utilities
loguru>=0.7.0
pyyaml>=6.0

# Providers (optional runtime integrations)
openai>=1.0.0

# Development / testing (optional - can be installed in dev env)
# pytest>=7.0.0
# jsonschema>=4.0.0
# black>=22.0.0
# Configuration Management
hydra-core>=1.3.0
omegaconf>=2.3.0

# Enhanced Logging
loguru>=0.7.0

# Core Python Dependencies (already available but explicit)
# These are typically built-in or standard requirements
pyyaml>=6.0
pathlib2>=2.3.7; python_version < "3.4"

# Optional: Enhanced development dependencies
# Uncomment if needed for development
# black>=22.0.0          # Code formatting
pytest>=7.0.0          # Testing framework
jsonschema>=4.0.0      # JSON schema validation for tests
# mypy>=1.0.0            # Type checking
requests>=2.28.0       # HTTP client used by firecracker client
fastapi>=0.95.0
uvicorn>=0.22.0
watchfiles>=0.19.0     # optional: efficient file watchers for SSE log streaming